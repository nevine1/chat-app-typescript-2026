import React from 'react'
import Login from '../src/components/auth/Login';

type Props = {}

const page = (props: Props) => {
    return (
        <div>
            <Login />
        </div>
    )
}

export default page;